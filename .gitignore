open-models/
coordinator/target/

# Portal runtime artifacts
portal/node_modules/
portal/dist/
portal/.pnpm-store/
portal/repos/
portal/status/
portal/tasks.json
portal/index.json
portal/.env

# Job registry (operational data, private)
jobs/
jobs.json
jobs.json.bak-*
scripts/matrix-cross-sign/node_modules/

# Local command sheet (canonical version is portal/public/commands/)
docs/repo-commands.html

# Plists contain BWS secret UUIDs — never commit
plists/

# Recovery stash: safety copies of docs that agents may damage during tool calls.
.stash/

# Archive — conversation logs, operational history
archive/

# Build artifacts and noise
__pycache__/
*.bak-*
*.bak
*.backup
portal/public/**/*.draft
portal/docs/*.html
portal-dist/

# Root-level Claude Code workspace
/.claude/

# OS noise
**/.DS_Store

# Obsidian workspace state (all vaults)
**/.obsidian/

# Agent content — separate repo (agents/), tracked independently
agents/
ig88-device-reset.sh

# Archives
*.zip
*.tar.gz
*.tgz

# Model weights — should live in HuggingFace, not git
*.safetensors
*.bin
*.gguf
*.pkl
*.pt
*.pth
*.onnx
