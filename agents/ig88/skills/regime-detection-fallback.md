[{'symbol': 'BTCUSDT', 'price': '78169.52000000'}, {'value': '46', 'manually': 'bash\n# Start with neutral baseline\nSCORE=5\n\n# Adjust based on Fear & Greed index\nif [', '$FG_VALUE" -lt 30 ]; then\n    SCORE=$((SCORE - 1))\nfi\n\n# Determine state based on score\nif [ "$SCORE" -ge 7 ]; then\n    STATE="RISK_ON"\nelif [ "$SCORE" -le 3 ]; then\n    STATE="RISK_OFF"\nelse\n    STATE="NEUTRAL': 'i\n```\n\n### 3. Create Regime Result Object\nFormat the result as JSON matching the expected output structure:\n\n```bash\nRESULT=', 'timestamp": "$(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)",\n  "regime": {\n    "state": "$STATE",\n    "score": $SCORE,\n    "confidence": 0.6\n  },\n  "btc_price": <btc_price>,\n  "fear_greed": $FG_VALUE\n}': 4.0, 'detection': 'bash\n# Define file paths\nCURRENT_REGIME_FILE=', '$RESULT" > "$CURRENT_REGIME_FILE"\n\n# Check if last regime exists\nif [ ! -f "$LAST_REGIME_FILE" ]; then\n    # First run - treat as change\n    cp "$CURRENT_REGIME_FILE" "$LAST_REGIME_FILE"\n    # Output the regime (changed state)\n    cat "$CURRENT_REGIME_FILE"\nelse\n    # Compare states\n    LAST_STATE=$(jq -r \'.regime.state\' "$LAST_REGIME_FILE")\n    CURRENT_STATE=$(jq -r \'.regime.state\' "$CURRENT_REGIME_FILE")\n    \n    if [ "$LAST_STATE" != "$CURRENT_STATE" ]; then\n        # Regime changed - output current regime JSON\n        cat "$CURRENT_REGIME_FILE"\n        \n        # Update last regime file\n        cp "$CURRENT_REGIME_FILE" "$LAST_REGIME_FILE"\n    else\n        # No change - report stability\n        echo "Regime stable.': 'i\nfi\n```\n\n## Example Output\n\n**When regime changes:**\n```json\n{', 'timestamp': '2026-04-23T14:48:02.123456+00:00', 'regime': {'state': 'NEUTRAL', 'score': 5, 'confidence': 0.6}, 'btc_price': 78169.52, 'fear_greed': 46}]